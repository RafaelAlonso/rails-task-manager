<%= form_for(@task) do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.label :done %>
  <%= f.check_box :done %>

  <%= f.submit 'Do it!', :class => 'btn btn-primary' %>
<% end %>
